# Core dependencies
# tkinter is part of the standard library and cannot be installed via pip
# Uncomment if using a package manager that supports system dependencies
# tkinter

# Image processing
pillow>=9.0.0

# System monitoring
psutil>=5.9.0

# VPK handling
vpk>=1.4.0

# Windows-specific dependencies
pywin32>=300

# Other dependencies
urllib3>=1.26.0  # For update service

# Note: The following are part of the standard library and cannot be installed via pip
# - tkinter
# - zipfile
# - zlib
# - lzma
